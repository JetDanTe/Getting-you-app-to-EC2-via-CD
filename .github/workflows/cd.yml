name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types: [completed]
    branches: [hw]  # explicitly specify branch

permissions:  # add explicit permissions
  contents: read
  actions: read
  checks: read
  deployments: write
  issues: read
  discussions: read
  packages: read
  pull-requests: read
  repository-projects: read
  security-events: read
  statuses: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CD Pipeline triggered"