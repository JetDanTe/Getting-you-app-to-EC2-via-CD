name: pip2

on:
  workflow_run:
    workflows: ["pip1"]
    types: [completed]
    branches: [hw] 

permissions:
  contents: read
  actions: read
  checks: read
  deployments: write
  issues: read
  discussions: read
  packages: read
  pull-requests: read
  repository-projects: read
  security-events: read
  statuses: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CD Pipeline triggered"